<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>kitdenatal-hiperdistribuidora.shop</title>
  <meta name="description" content="Kit de Natal - Hiper Distribuidora | Produtos, promoções e envio rápido" />

  <!-- Open Graph / Social -->
  <meta property="og:title" content="kitdenatal-hiperdistribuidora.shop" />
  <meta property="og:description" content="Kit de Natal - Hiper Distribuidora | Produtos, promoções e envio rápido" />
  <meta property="og:type" content="website" />

  <style>
    /* Minimal, clean one-file presentation for GitHub Pages */
    :root{
      --bg:#0f1724; --card:#0b1220; --accent:#f97316; --muted:#9ca3af; --glass: rgba(255,255,255,0.04);
      --radius:16px; --maxw:980px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family:Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: linear-gradient(180deg,#021226 0%, #041028 60%); color:#e6eef8; -webkit-font-smoothing:antialiased;
      display:flex; align-items:center; justify-content:center; padding:48px;
    }
    .wrap{width:100%; max-width:var(--maxw)}
    .card{background:linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.02)); padding:36px; border-radius:var(--radius); box-shadow:0 10px 30px rgba(2,6,23,0.6);
      backdrop-filter: blur(6px); border:1px solid rgba(255,255,255,0.03);
    }
    header{display:flex; gap:16px; align-items:center;}
    .logo{width:74px; height:74px; border-radius:12px; background:linear-gradient(135deg,var(--accent),#ffb86b); display:flex; align-items:center; justify-content:center; font-weight:700; color:#08202a}
    h1{margin:0; font-size:20px}
    p.lead{margin:6px 0 0; color:var(--muted); font-size:14px}

    .grid{display:grid; grid-template-columns:1fr 320px; gap:24px; margin-top:22px}
    .hero{padding:18px;}
    .features{display:flex; flex-direction:column; gap:12px; margin-top:18px}
    .feature{background:var(--glass); padding:12px; border-radius:12px; border:1px solid rgba(255,255,255,0.02)}
    .btn{display:inline-block; padding:10px 14px; border-radius:10px; background:linear-gradient(90deg,var(--accent),#ffb86b); color:#071218; font-weight:600; text-decoration:none}

    aside{padding:18px; background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01)); border-radius:12px}
    .meta{font-size:13px; color:var(--muted)}
    footer{margin-top:18px; text-align:center; color:var(--muted); font-size:13px}

    @media (max-width:900px){
      .grid{grid-template-columns:1fr;}
      body{padding:20px}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <header>
        <div class="logo">KD</div>
        <div>
          <h1>kitdenatal-hiperdistribuidora.shop</h1>
          <p class="lead">Apresentação do projeto • Repositório configurado para GitHub Pages</p>
        </div>
      </header>

      <div class="grid">
        <main class="hero">
          <section>
            <h2>Sobre o site</h2>
            <p class="meta">Este repositório contém uma página estática (index.html) pensada para publicar via GitHub Pages no domínio <strong>kitdenatal-hiperdistribuidora.shop</strong>.</p>
            <div class="features">
              <div class="feature">
                <strong>Design responsivo</strong>
                <div class="meta">Funciona bem em desktop e mobile — tudo numa única página HTML.</div>
              </div>
              <div class="feature">
                <strong>Metadados para SEO</strong>
                <div class="meta">Tags Open Graph e description para compartilhamento em redes.</div>
              </div>
              <div class="feature">
                <strong>Fácil deploy</strong>
                <div class="meta">Basta colocar este arquivo em <code>index.html</code> na raiz do repositório e ativar o GitHub Pages.</div>
              </div>
            </div>

            <p style="margin-top:18px;"><a class="btn" href="#contact">Entrar em contato</a></p>
          </section>

          <section style="margin-top:22px;">
            <h3 id="contact">Contato / Próximos passos</h3>
            <p class="meta">Se quiser, eu adapto esse HTML com suas cores, logo, textos de produto e links para o catálogo. Posso também gerar um arquivo <code>CNAME</code> pronto com o domínio.</p>
          </section>
        </main>

        <aside>
          <div style="display:flex; justify-content:space-between; align-items:center; gap:12px;">
            <div>
              <div class="meta">Status do deploy</div>
              <strong>Pronto para GitHub Pages</strong>
            </div>
            <div style="text-align:right">
              <div class="meta">Domínio alvo</div>
              <div><strong>kitdenatal-hiperdistribuidora.shop</strong></div>
            </div>
          </div>

          <hr style="margin:14px 0; border:none; height:1px; background:rgba(255,255,255,0.02)" />

          <div>
            <div class="meta">Como publicar</div>
            <ol style="padding-left:18px; margin:8px 0 0; color:var(--muted); font-size:13px">
              <li>Salvar este arquivo como <code>index.html</code> na raiz do repositório.</li>
              <li>Adicionar um arquivo <code>CNAME</code> com uma linha: <code>kitdenatal-hiperdistribuidora.shop</code></li>
              <li>Fazer commit e push para a branch escolhida (ex: <code>main</code>).</li>
              <li>Ativar GitHub Pages nas configurações do repositório (branch: main / pasta: root).</li>
            </ol>
          </div>

        </aside>
      </div>

      <footer>
        © Hiper Distribuidora — Página de demonstração • Gerado automaticamente
      </footer>
    </div>
  </div>
</body>
</html>
